# Environment variables / secrets
.env
.env.*
!.env.example

# Local DB + ingest temp
data/
evals/last_results.json
deploy_artifacts/
# SSH private keys for Render shell (public can be shared)
data/ssh/
!data/ssh/*.pub
# Temporary secret-rewrite helpers (never commit)
data/_secret_replacements.txt

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Local temp / books (do not commit copyrighted PDFs)
temp_*.pdf
*.pdf

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

.aiogram_probe/
