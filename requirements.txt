# Notbook Telegram bot (main service on Render / Docker)
# Use Python 3.11 (see runtime.txt) — avoid 3.14 for pydantic-core wheels.
# Bot API 10.1+ rich messages + drafts (sendRichMessage / sendRichMessageDraft)
aiogram==3.29.1
pyyaml==6.0.2
google-genai>=1.0.0
pinecone==5.4.2
pydantic==2.9.2
aiohttp==3.10.10
python-dotenv==1.0.1
pypdfium2>=4.30.0
# Optional local cross-encoder reranker (heavy: torch)
# sentence-transformers>=3.0.0
